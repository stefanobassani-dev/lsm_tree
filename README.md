# LSM Tree

## Compile and build

If you don't have CMake, install it.

`sudo apt install cmake`

Then proceed with

* `mkdir build`
* `cd build`
* `cmake ..`
* `cmake --build .`

## Command

* **insert**: `p \<key\> \<value\>`
* **search**: `g \<key\>`
* **quit**: `q`
